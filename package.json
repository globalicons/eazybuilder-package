{
  "name": "eazybuild-reactpackage",
  "version": "1.0.1",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "description": "A reusable React components package.",
  "keywords": [
    "react",
    "components",
    "UI",
    "eazybuild"
  ],
  "author": "Charles Bryan Oware",
  "license": "ISC", 
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && babel src --out-dir dist --copy-files",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "antd": "^5.23.2",
    "react": ">=16.8",
    "react-dom": ">=16.8"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.10",
    "@babel/core": "^7.22.10",
    "@babel/preset-env": "^7.22.15",
    "@babel/preset-react": "^7.22.5"
  }
}
